# Background workers

Use the same API image with a separate command when a real asynchronous workload exists (notifications, media, ratings, billing). Do not add a permanently idle worker container before the first queue-backed feature.
