# Alembic migrations

Each revision must declare its owning domain module in the filename and header, for example `20260719_programs_initial.py`. Run migrations as a dedicated deployment step, never implicitly in every API container startup.
